
<% if @feed_items.any? %>
  <ol class="microposts">
    <!-- Here we use partial explicitly because the collection parameter -->
    <%= render partial: 'shared/feed_item', collection: @feed_items %>
  </ol>
  <%= will_paginate @feed_items %>
<% end %>
